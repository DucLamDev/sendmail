# EmailJS Configuration
# Get your credentials from: https://dashboard.emailjs.com/admin
# IMPORTANT: Enable "Allow EmailJS API for non-browser applications" in Security settings
EMAILJS_SERVICE_ID=your_service_id
EMAILJS_TEMPLATE_ID=your_template_id
# Private Key (Access Token) is REQUIRED for server-side API calls
# Get it from: Dashboard â†’ Account â†’ Security
EMAILJS_PRIVATE_KEY=your_private_key_here
# Public Key is optional (can use Private Key instead)
EMAILJS_PUBLIC_KEY=your_public_key_here

# Server Configuration
PORT=3000
